packages:
  - "."

# pnpm 10+ blocks dependency install scripts unless allowed here
# (native binaries for the Next.js compiler, the ESLint resolver and file watching).
allowBuilds:
  "@swc/core": true
  "@parcel/watcher": true
  unrs-resolver: true
